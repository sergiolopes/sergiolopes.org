/* analytics removed during archive migration */
// load twitter if needed
(function(){
	if (document.querySelectorAll('.tweetable').length > 0) {
		/* social widget loading removed from the static archive */
	}
})();
